"use strict " // treat all JS code in newer version

// alert (3+3) we are using nodejs, not browser

console.log(3+3);
console.log("Pranav")

let name = "Pranav"
let age = 20
let isLoggedin = "True"
let state;

/*
Primitive Datatypes

name = string // string should be in "" quotes
isLoggedin = boolean // it returns true of false value
null = standalone value (object)
undefined =  means value is not assigned
symbol = used to find uniqueness, used in reactjs

*/

console.log(typeof undefined) // returns undefined value
console.log(typeof null) // returns null value